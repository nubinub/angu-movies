import { MediaVotePipe } from './media-vote.pipe';

describe('MediaVotePipe', () => {
  it('create an instance', () => {
    const pipe = new MediaVotePipe();
    expect(pipe).toBeTruthy();
  });
});
